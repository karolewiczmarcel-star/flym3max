-- NiggaDestroyer GUI v2 - NA PEWNO DZIAŁA
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local rootPart = character:WaitForChild("HumanoidRootPart")
local camera = workspace.CurrentCamera

local flying = false
local noclip = false
local flySpeed = 60
local bodyVelocity

------------------------------------------------------------
-- 🛑 NAJPIERW USUŃ STARE GUI (jeśli istnieje)
------------------------------------------------------------
if playerGui:FindFirstChild("FlyGui") then
    playerGui.FlyGui:Destroy()
end
if playerGui:FindFirstChild("NiggaDestroyerGui") then
    playerGui.NiggaDestroyerGui:Destroy()
end

------------------------------------------------------------
-- 🎨 TWORZENIE NOWEGO GUI
------------------------------------------------------------
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "NiggaDestroyerGui"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = playerGui

local frame = Instance.new("Frame")
frame.Name = "Main"
frame.Size = UDim2.new(0, 280, 0, 310)
frame.Position = UDim2.new(0, 20, 0, 100)
frame.BackgroundColor3 = Color3.fromRGB(20, 0, 30)
frame.BorderSizePixel = 0
frame.Visible = false
frame.Parent = screenGui
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 12)

-- ✨ GRADIENT TŁA (fioletowo-czarny)
local bgGradient = Instance.new("UIGradient")
bgGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(120, 0, 180)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(50, 0, 80)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(15, 0, 30))
})
bgGradient.Rotation = 45
bgGradient.Parent = frame

-- 🔥 TYTUŁ
local title = Instance.new("TextLabel")
title.Name = "Title"
title.Size = UDim2.new(1, -20, 0, 45)
title.Position = UDim2.new(0, 10, 0, 5)
title.BackgroundTransparency = 1
title.Text = "🔥 NiggaDestroyer 🔥"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextStrokeColor3 = Color3.fromRGB(200, 50, 0)
title.TextStrokeTransparency = 0
title.TextSize = 24
title.Font = Enum.Font.GothamBold
title.Parent = frame

local titleGrad = Instance.new("UIGradient")
titleGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 220, 0)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 100, 0)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 30, 0))
})
titleGrad.Parent = title

------------------------------------------------------------
-- 🛠️ FUNKCJA DO PRZYCISKÓW
------------------------------------------------------------
local function makeButton(name, text, yPos, gradColor1, gradColor2)
    local btn = Instance.new("TextButton")
    btn.Name = name
    btn.Size = UDim2.new(0.85, 0, 0, 42)
    btn.Position = UDim2.new(0.075, 0, 0, yPos)
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 16
    btn.Text = text
    btn.AutoButtonColor = true
    btn.Parent = frame
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)
    
    local g = Instance.new("UIGradient")
    g.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, gradColor1),
        ColorSequenceKeypoint.new(1, gradColor2)
    })
    g.Rotation = 90
    g.Parent = btn
    return btn
end

-- 🛩️ FLY
local flyButton = makeButton("FlyBtn", "🔥 Fly: OFF", 60,
    Color3.fromRGB(220, 40, 40),
    Color3.fromRGB(120, 10, 10))

-- ⚡ NOCLIP
local noclipButton = makeButton("NoclipBtn", "⚡ Noclip: OFF", 115,
    Color3.fromRGB(230, 150, 0),
    Color3.fromRGB(140, 80, 0))

------------------------------------------------------------
-- 📏 SPEED LABEL + SLIDER
------------------------------------------------------------
local speedLabel = Instance.new("TextLabel")
speedLabel.Name = "SpeedLabel"
speedLabel.Size = UDim2.new(0.85, 0, 0, 25)
speedLabel.Position = UDim2.new(0.075, 0, 0, 175)
speedLabel.BackgroundTransparency = 1
speedLabel.TextColor3 = Color3.fromRGB(255, 200, 100)
speedLabel.Text = "⚡ Speed: 60"
speedLabel.TextSize = 16
speedLabel.Font = Enum.Font.GothamBold
speedLabel.Parent = frame

local sliderBack = Instance.new("Frame")
sliderBack.Name = "SliderBack"
sliderBack.Size = UDim2.new(0.85, 0, 0, 14)
sliderBack.Position = UDim2.new(0.075, 0, 0, 210)
sliderBack.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
sliderBack.Parent = frame
Instance.new("UICorner", sliderBack).CornerRadius = UDim.new(0, 7)

local sliderFill = Instance.new("Frame")
sliderFill.Name = "SliderFill"
sliderFill.Size = UDim2.new(0.3, 0, 1, 0)
sliderFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
sliderFill.Parent = sliderBack
Instance.new("UICorner", sliderFill).CornerRadius = UDim.new(0, 7)

local sliderGrad = Instance.new("UIGradient")
sliderGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 220, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(180, 0, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 180))
})
sliderGrad.Parent = sliderFill

------------------------------------------------------------
-- 💬 STATUS
------------------------------------------------------------
local statusLabel = Instance.new("TextLabel")
statusLabel.Name = "Status"
statusLabel.Size = UDim2.new(0.85, 0, 0, 30)
statusLabel.Position = UDim2.new(0.075, 0, 0, 240)
statusLabel.BackgroundTransparency = 1
statusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
statusLabel.Text = "💀 Ready to destroy 💀"
statusLabel.TextSize = 13
statusLabel.Font = Enum.Font.Gotham
statusLabel.Parent = frame

local footer = Instance.new("TextLabel")
footer.Name = "Footer"
footer.Size = UDim2.new(1, 0, 0, 22)
footer.Position = UDim2.new(0, 0, 1, -25)
footer.BackgroundTransparency = 1
footer.TextColor3 = Color3.fromRGB(180, 80, 220)
footer.Text = "⚡ Press P to toggle menu ⚡"
footer.TextSize = 12
footer.Font = Enum.Font.GothamBold
footer.Parent = frame

------------------------------------------------------------
-- 🖱️ SLIDER LOGIC
------------------------------------------------------------
local dragging = false
sliderBack.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
    end
end)
sliderBack.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement
    or input.UserInputType == Enum.UserInputType.Touch) then
        local absPos = sliderBack.AbsolutePosition.X
        local absSize = sliderBack.AbsoluteSize.X
        local mouseX = input.Position.X
        local ratio = math.clamp((mouseX - absPos) / absSize, 0, 1)
        sliderFill.Size = UDim2.new(ratio, 0, 1, 0)
        flySpeed = math.floor(20 + ratio * 180)
        speedLabel.Text = "⚡ Speed: " .. flySpeed
    end
end)

------------------------------------------------------------
-- 🛩️ FLY LOGIC
------------------------------------------------------------
local function updateStatus()
    if flying and noclip then
        statusLabel.Text = "🔥🔥 FULL POWER MODE 🔥🔥"
    elseif flying then
        statusLabel.Text = "✈️ Flying like a god ✈️"
    elseif noclip then
        statusLabel.Text = "⚡ Noclip active ⚡"
    else
        statusLabel.Text = "💀 Ready to destroy 💀"
    end
end

local function startFlying()
    if flying then return end
    flying = true
    humanoid.PlatformStand = true
    flyButton.Text = "🔥 Fly: ON"
    if bodyVelocity then bodyVelocity:Destroy() end
    bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bodyVelocity.Velocity = Vector3.new(0, 0, 0)
    bodyVelocity.P = 1250
    bodyVelocity.Parent = rootPart
    updateStatus()
end

local function stopFlying()
    if not flying then return end
    flying = false
    humanoid.PlatformStand = false
    flyButton.Text = "🔥 Fly: OFF"
    if bodyVelocity then bodyVelocity:Destroy() bodyVelocity = nil end
    updateStatus()
end

flyButton.MouseButton1Click:Connect(function()
    if flying then stopFlying() else startFlying() end
end)

------------------------------------------------------------
-- ⚡ NOCLIP LOGIC
------------------------------------------------------------
noclipButton.MouseButton1Click:Connect(function()
    noclip = not noclip
    if noclip then
        noclipButton.Text = "⚡ Noclip: ON"
    else
        noclipButton.Text = "⚡ Noclip: OFF"
    end
    updateStatus()
end)

------------------------------------------------------------
-- ⌨️ TOGGLE GUI
------------------------------------------------------------
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return
